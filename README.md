## A (tiny) node program for reversing the lines order in a file

### to run:
```> revFile <fileName>```

### installation
you can if you want, to run ```npm link``` on the project dir after clone in order to execute from any directory.
